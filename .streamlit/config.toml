[server]
maxUploadSize = 200
headless = true
# Required for file upload when app runs in iframe (e.g. Hugging Face Spaces)
enableXsrfProtection = false
enableCORS = false

[theme]
base = "dark"
primaryColor = "#76B900"
